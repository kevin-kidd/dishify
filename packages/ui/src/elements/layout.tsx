import { View } from "react-native"
import { withWebTag } from "../utils/web-tag"

{
  /* Lists */
}
export const UnorderedList = withWebTag(View, "ul")
export const OrderedList = withWebTag(View, "ol")
export const ListItem = withWebTag(View, "li")
